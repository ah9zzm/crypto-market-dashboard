{
  "name": "crypto-market-dashboard",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev-all.mjs",
    "dev:web": "npm run dev -w @cmd/web",
    "dev:api": "npm run start:dev -w @cmd/api",
    "dev:worker": "npm run dev -w @cmd/worker",
    "build": "npm run build -w @cmd/shared-types && npm run build -w @cmd/api && npm run build -w @cmd/worker && npm run build -w @cmd/web",
    "lint": "npm run lint -w @cmd/web",
    "typecheck": "npm run typecheck -w @cmd/shared-types && npm run typecheck -w @cmd/api && npm run typecheck -w @cmd/worker && npm run typecheck -w @cmd/web",
    "test:api": "npm run test -w @cmd/api"
  }
}
